$("#pre, .pre-loupe img").attr("src","<%= @image.full_url %>");

gPrePosition = <%= @image.position %>;
gPreId = <%= @image.id %>;

$("#pre-thumb0").attr("src","<%= @thumbs[0] %>");
$("#pre-thumb1").attr("src","<%= @thumbs[1] %>");
$("#pre-thumb2").attr("src","<%= @thumbs[2] %>");

$(".pre h1").html("<%= escape_javascript(render(partial: 'title')) %>");

iCoast.matchExists(gPreId,gPostId,function(exists) {
	$("#match_yes").prop("checked",exists);
	$("#match_no").prop("checked",!exists);
});
