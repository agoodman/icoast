console.log("loading pre image <%= @image.full_url %>");
$("#pre").attr("src","<%= @image.full_url %>");

gPrePosition = <%= @image.position %>;
gPreId = <%= @image.id %>;

$("#pre-thumb0").attr("src","<%= @thumbs[0] %>");
$("#pre-thumb1").attr("src","<%= @thumbs[1] %>");
$("#pre-thumb2").attr("src","<%= @thumbs[2] %>");

$(".pre h1").html("PRE-STORM at (<%= @image.latitude %>, <%= @image.longitude %>)");

matchExists(gPreId,gPostId,function(exists) {
	$("#match").prop("checked",exists);
});
